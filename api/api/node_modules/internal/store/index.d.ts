import { Immutable } from '../ts/type';
export declare type Store<S> = Immutable<S> & Readonly<Mixin<S>>;
export declare type Action<S> = (state: Immutable<S>) => Partial<S>;
declare type Mixin<S> = {
    subscribe: (fn: () => void) => void;
    unsubscribe: (fn: () => void) => void;
    setState(state: Partial<S> | Action<S>): void;
    toJSON(): Immutable<S>;
};
export default function Store<S>(initial: Immutable<S>): Store<S>;
export {};
